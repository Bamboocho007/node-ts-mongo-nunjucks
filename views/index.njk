<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    Hellow {{ user.firstName if user else "Stranger" }}!
    {% if user %}
        User content 
    {% else %}
        Stranger content
    {% endif %}
</body> 
</html>